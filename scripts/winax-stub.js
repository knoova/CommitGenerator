/**
 * Stub for 'winax' - Windows-only ActiveX/COM module.
 * Used when bundling for non-Windows (e.g. macOS). SapiTTS is never used
 * when engine is 'vits', so this stub is never called at runtime.
 */
module.exports = { default: {} };
